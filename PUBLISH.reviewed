# 发布闸的「看过了，没问题」清单。
#
# scripts/oss-scan.mjs 命中的**原文**写在这里，就不再报。一行一条，`#` 后面写理由。
#
# ⚠ 规矩两条：
#   ① 一条只放一个**具体字符串**，不许放正则、不许放前缀——放宽一次，以后就永远宽着。
#   ② 每条必须写理由。写不出理由 = 这条其实该改掉，不该放行。

# ---- 明显是假的示例地址（测的就是「域名匹配对不对」，必须长这样）----
a@b.com                   # 解析地址那几条测试的最小样例
x@y.com                   # 同上
<a@b.com>                 # 同上，带尖括号那种写法
x@notcorp-mail.com        # 绊线：后缀相同但不是子域，必须判成外部
x@corp-mail.com.evil.cn   # 绊线：域名在中间不算内部
corp-mail.com@gmail.com   # 绊线：域名出现在本地部不算内部
x@mail.corp-mail.com      # 绊线：真子域算内部
me@acme.com               # 夹具邮箱，acme.com 是保留的示例域名
me@corp-mail.com          # 同上
feng.zhang@corp-mail.com  # 夹具邮箱（人名已换成虚构的）
ming.li@corp-mail.com     # 同上
zhang.san@corp.com        # 同上，botAddress 测试用的虚构真人地址（张三是通用示例名）
client@example.com        # 夹具：外部客户
crm-notify@corp-mail.com  # 夹具：工单系统发信地址

# ---- 平台/产品自己的公开地址，不是个人信息 ----
www.mingdao.com           # 明道云 SaaS 的公开站点，是这个产品的地址，配置里也能改成私有部署地址
明道云                     # 支持的平台名字，文档里必须写出来
明道                       # 同上（简称）

# ---- 公开产品名，不是个人信息 ----
Nocoly                    # 明道云的国际版品牌，公开产品名（注释和夹具里当群名/项目名用）
nocoly                    # 同上（小写，出现在项目目录名 P11-nocoly-sea-marketing 里）

# ---- botAddress 判据说明里的真实/虚构示例地址（不是任何人的私信地址）----
noreply-dmarc-support@google.com   # Google 自己发 DMARC 汇总报告的公开地址，非个人
dmarc@google.com                   # 同上
alerta.perez@corp.com              # 虚构人名（alerta=西语「警报」双关），corp.com 是保留示例域名，用来测「地址含机器人词但其实是真人」这条判据

# ---- 明显编的夹具 UUID ----
00000000-0000-4000-8000-000000000001   # 测试用的假任务 id，一眼看得出是编的
00000000-0000-4000-8000-000000000002   # 同上
00000000-0000-4000-8000-000000000003   # 同上

# ---- 虚构人物（夹具里统一用这几个）----
王小明   # 夹具里的「本人」
小明     # 同上，称呼
李雷     # 夹具里的同事
雷哥     # 李雷的 nickname
韩梅     # 夹具里的同事
梅姐     # 韩梅的 nickname
李大雷   # 夹具里的同事（本名里含 nickname，专门用来测那条判据）
大雷     # 李大雷的 nickname
张总     # 泛指的职务尊称，测称呼门用的，不指任何人
领导     # 同上

# ---- L3 的误报（「@提及」这个词本身被当成了提及）----
提及
提及一起
提及还回
提及开头
某某
了你

# ---- 年度目标代号，不是公司名 ----
G2   # goals.md 里的目标代号（G0–G5），夹具里当项目归属用

# ---- 附件/群测试夹具里的假 UUID（2026-08-18 脱敏时特意造的，规律型一眼假）----
11112222-3333-4444-5555-666677778888   # group-initiate 测试的假 groupId
aaaabbbb-cccc-dddd-eeee-ffff00001111   # hap-task-comment 测试的假 accountId
人要在正                                # connect/hap.mjs 注释「@人要在正文里写…」的误切片，不是人名
姓名                                   # 注释「渲染成 @姓名」的通用词，不是具体人
